import fs from 'node:fs';
import path from 'node:path';
const root=path.resolve('dist');
function walk(dir){return fs.readdirSync(dir,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(path.join(dir,e.name)):[path.join(dir,e.name)])}
if(fs.existsSync(root))for(const file of walk(root)){if(!file.endsWith('.html'))continue;let html=fs.readFileSync(file,'utf8');const head=[];const body=[];if(!html.includes('/relay-shell.css'))head.push('<link rel="stylesheet" href="/relay-shell.css">');if(!html.includes('/relay-nav.js'))body.push('<script src="/relay-nav.js"></script>');if(!html.includes('/relay-command.js'))body.push('<script src="/relay-command.js"></script>');if(head.length)html=html.includes('</head>')?html.replace('</head>',head.join('')+'</head>'):head.join('')+html;if(body.length)html=html.includes('</body>')?html.replace('</body>',body.join('')+'</body>'):html+'\n'+body.join('')+'\n';fs.writeFileSync(file,html)}
console.log('RelayOS unified shell, navigation and AI command center injected into HTML build output');