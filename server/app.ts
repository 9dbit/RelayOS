import express from 'express';
import crypto from 'crypto';
import {spawn} from 'child_process';
import {pool} from './db.js';
import {migrateKnowledge,registerKnowledgeRoutes} from './knowledge.js';
import {migrateHybridRag,registerHybridRagRoutes} from './hybrid-rag.js';
import {migrateCrm,registerCrmRoutes} from './crm.js';
import {migrateCrmIntelligence,registerCrmIntelligenceRoutes} from './crm-intelligence.js';
import {migrateContinuity,registerContinuityRoutes} from './continuity.js';

const app=express();app.set('trust proxy',1);app.use(express.json({limit:'10mb'}));
const publicPort=Number(process.env.PORT||3000),internalPort=Number(process.env.RELAYOS_INTERNAL_PORT||3001),secret=process.env.RELAYOS_JWT_SECRET||'',bootstrapToken=process.env.RELAYOS_BOOTSTRAP_TOKEN||'',cookieName='relayos_session',sessionHours=12;
if(secret.length<32)throw new Error('RELAYOS_JWT_SECRET must be configured with at least 32 characters');
function b64(v:Buffer|string){return Buffer.from(v).toString('base64url')}
function signJwt(p:any){const h=b64(JSON.stringify({alg:'HS256',typ:'JWT'})),b=b64(JSON.stringify(p)),s=crypto.createHmac('sha256',secret).update(`${h}.${b}`).digest('base64url');return`${h}.${b}.${s}`}
function verifyJwt(t:string){try{const[h,b,s]=t.split('.');if(!h||!b||!s)return null;const e=crypto.createHmac('sha256',secret).update(`${h}.${b}`).digest('base64url');if(s.length!==e.length||!crypto.timingSafeEqual(Buffer.from(s),Buffer.from(e)))return null;const p=JSON.parse(Buffer.from(b,'base64url').toString());return p.exp&&Date.now()/1000<p.exp?p:null}catch{return null}}
function cookies(req:express.Request){return Object.fromEntries(String(req.headers.cookie||'').split(';').map(x=>x.trim()).filter(Boolean).map(v=>{const i=v.indexOf('=');return[decodeURIComponent(v.slice(0,i)),decodeURIComponent(v.slice(i+1))]}))}
function tokenHash(v:string){return crypto.createHash('sha256').update(v).digest('hex')}
async function scryptHash(p:string,s=crypto.randomBytes(16).toString('hex')){return new Promise<string>((ok,bad)=>crypto.scrypt(p,s,64,(e,k)=>e?bad(e):ok(`${s}:${Buffer.from(k).toString('hex')}`)))}
async function verifyPassword(p:string,stored:string){try{const[s]=stored.split(':');const f=await scryptHash(p,s);return f.length===stored.length&&crypto.timingSafeEqual(Buffer.from(f),Buffer.from(stored))}catch{return false}}
function setCookie(res:express.Response,t:string){res.setHeader('Set-Cookie',`${cookieName}=${encodeURIComponent(t)}; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=${sessionHours*3600}`)}
function clearCookie(res:express.Response){res.setHeader('Set-Cookie',`${cookieName}=; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=0`)}
function externalOrigin(req:express.Request){const p=String(req.headers['x-forwarded-proto']||req.protocol).split(',')[0].trim(),h=String(req.headers['x-forwarded-host']||req.get('host')||'').split(',')[0].trim();return`${p}://${h}`}
function checkOrigin(req:express.Request,res:express.Response){if(['GET','HEAD','OPTIONS'].includes(req.method))return true;const o=String(req.headers.origin||'');if(!o)return true;if(o!==externalOrigin(req)){res.status(403).json({error:'origin_rejected'});return false}return true}
async function migrateAuth(){await pool.query(`alter table operators add column if not exists password_hash text;alter table operators add column if not exists auth_version int not null default 1;alter table operators add column if not exists last_login_at timestamptz;alter table operators add column if not exists invited_at timestamptz;alter table operators add column if not exists accepted_at timestamptz;create unique index if not exists uq_operator_email_ci on operators(lower(email)) where email is not null;create table if not exists auth_sessions(id uuid primary key default gen_random_uuid(),operator_id uuid not null references operators(id) on delete cascade,jti text not null unique,expires_at timestamptz not null,revoked_at timestamptz,ip_hash text,user_agent text,created_at timestamptz not null default now(),last_seen_at timestamptz not null default now());create table if not exists operator_invites(id uuid primary key default gen_random_uuid(),operator_id uuid not null references operators(id) on delete cascade,token_hash text not null unique,invited_by uuid references operators(id),expires_at timestamptz not null,accepted_at timestamptz,created_at timestamptz not null default now());create index if not exists idx_auth_sessions_operator on auth_sessions(operator_id,expires_at);`)}
async function currentUser(req:express.Request){const t=cookies(req)[cookieName];if(!t)return null;const j=verifyJwt(t);if(!j?.sub||!j?.jti)return null;const q=await pool.query(`select o.id,o.name,o.email,o.role,o.department,o.presence,o.active,o.auth_version,s.id session_id,s.expires_at from auth_sessions s join operators o on o.id=s.operator_id where s.jti=$1 and s.operator_id=$2 and s.revoked_at is null and s.expires_at>now() and o.active=true`,[j.jti,j.sub]);const u=q.rows[0];if(!u||Number(j.ver)!==Number(u.auth_version))return null;await pool.query('update auth_sessions set last_seen_at=now() where id=$1',[u.session_id]);return u}
async function requireUser(req:express.Request,res:express.Response){const u=await currentUser(req);if(!u){req.path.startsWith('/api/')?res.status(401).json({error:'authentication_required'}):res.redirect('/login.html');return null}return u}

app.get('/api/auth/me',async(req,res)=>{const u=await currentUser(req);u?res.json({user:u}):res.status(401).json({error:'authentication_required'})});
app.post('/api/auth/login',async(req,res)=>{if(!checkOrigin(req,res))return;const email=String(req.body?.email||'').trim().toLowerCase(),password=String(req.body?.password||'');const u=(await pool.query('select * from operators where lower(email)=$1 and active=true',[email])).rows[0];if(!u?.password_hash||!(await verifyPassword(password,u.password_hash)))return res.status(401).json({error:'invalid_credentials'});const jti=crypto.randomBytes(24).toString('hex'),now=Math.floor(Date.now()/1000),exp=now+sessionHours*3600;await pool.query('insert into auth_sessions(operator_id,jti,expires_at,ip_hash,user_agent) values($1,$2,to_timestamp($3),$4,$5)',[u.id,jti,exp,tokenHash(String(req.ip||'')),String(req.headers['user-agent']||'').slice(0,500)]);await pool.query("update operators set last_login_at=now(),last_seen_at=now(),presence='online' where id=$1",[u.id]);setCookie(res,signJwt({sub:u.id,jti,role:u.role,ver:u.auth_version,iat:now,exp}));res.json({ok:true,user:{id:u.id,name:u.name,email:u.email,role:u.role,department:u.department}})});
app.post('/api/auth/logout',async(req,res)=>{if(!checkOrigin(req,res))return;const j=verifyJwt(cookies(req)[cookieName]||'');if(j?.jti)await pool.query('update auth_sessions set revoked_at=now() where jti=$1',[j.jti]);clearCookie(res);res.json({ok:true})});
app.post('/api/auth/bootstrap',async(req,res)=>{if(!checkOrigin(req,res))return;if(!bootstrapToken||String(req.body?.token||'')!==bootstrapToken)return res.status(403).json({error:'invalid_bootstrap_token'});const n=(await pool.query('select count(*)::int n from operators where password_hash is not null')).rows[0].n;if(n>0)return res.status(409).json({error:'bootstrap_already_completed'});const email=String(req.body?.email||'').trim().toLowerCase(),password=String(req.body?.password||''),name=String(req.body?.name||'Admin').trim();if(!email.includes('@')||password.length<10)return res.status(400).json({error:'email_and_10_char_password_required'});const hash=await scryptHash(password);const q=await pool.query("update operators set name=$1,email=$2,password_hash=$3,accepted_at=now(),role='admin',active=true where id=(select id from operators where role='admin' order by created_at limit 1) returning id,name,email,role",[name,email,hash]);res.json({ok:true,user:q.rows[0]})});
app.post('/api/auth/invites',async(req,res)=>{if(!checkOrigin(req,res))return;const a=await requireUser(req,res);if(!a)return;if(!['admin','supervisor'].includes(a.role))return res.status(403).json({error:'forbidden'});const email=String(req.body?.email||'').trim().toLowerCase(),name=String(req.body?.name||'').trim(),role=String(req.body?.role||'operator'),department=String(req.body?.department||'').trim()||null;if(!email.includes('@')||!name||!['admin','supervisor','operator'].includes(role))return res.status(400).json({error:'invalid_invite'});if(a.role==='supervisor'&&role!=='operator')return res.status(403).json({error:'supervisor_can_only_invite_operator'});const op=(await pool.query(`insert into operators(name,email,role,department,presence,active,invited_at) values($1,$2,$3,$4,'offline',true,now()) on conflict(lower(email)) where email is not null do update set name=excluded.name,department=excluded.department returning id,name,email,role,department`,[name,email,role,department])).rows[0];const raw=crypto.randomBytes(32).toString('base64url');await pool.query("update operator_invites set accepted_at=now() where operator_id=$1 and accepted_at is null",[op.id]);await pool.query("insert into operator_invites(operator_id,token_hash,invited_by,expires_at) values($1,$2,$3,now()+interval '48 hours')",[op.id,tokenHash(raw),a.id]);res.status(201).json({ok:true,operator:op,invite_url:`${externalOrigin(req)}/invite.html?token=${encodeURIComponent(raw)}`,expires_in_hours:48})});
app.post('/api/auth/invites/accept',async(req,res)=>{if(!checkOrigin(req,res))return;const raw=String(req.body?.token||''),password=String(req.body?.password||'');if(password.length<10)return res.status(400).json({error:'password_min_10_chars'});const inv=(await pool.query(`select i.*,o.email,o.name from operator_invites i join operators o on o.id=i.operator_id where i.token_hash=$1 and i.accepted_at is null and i.expires_at>now()`,[tokenHash(raw)])).rows[0];if(!inv)return res.status(400).json({error:'invite_invalid_or_expired'});const hash=await scryptHash(password),c=await pool.connect();try{await c.query('begin');await c.query('update operators set password_hash=$1,accepted_at=now(),auth_version=auth_version+1 where id=$2',[hash,inv.operator_id]);await c.query('update operator_invites set accepted_at=now() where id=$1',[inv.id]);await c.query('commit')}catch(e){await c.query('rollback');throw e}finally{c.release()}res.json({ok:true,email:inv.email,name:inv.name})});
app.post('/api/auth/sessions/revoke-all',async(req,res)=>{if(!checkOrigin(req,res))return;const a=await requireUser(req,res);if(!a)return;await pool.query('update auth_sessions set revoked_at=now() where operator_id=$1 and revoked_at is null',[a.id]);await pool.query('update operators set auth_version=auth_version+1 where id=$1',[a.id]);clearCookie(res);res.json({ok:true})});

app.use('/api/v1/knowledge',async(req,res,next)=>{if(!checkOrigin(req,res))return;const u=await requireUser(req,res);if(!u)return;req.headers['x-relayos-operator-id']=String(u.id);next()});
registerKnowledgeRoutes(app);registerHybridRagRoutes(app,{requireUser});registerCrmRoutes(app,{requireUser});registerCrmIntelligenceRoutes(app,{requireUser});registerContinuityRoutes(app,{requireUser});

const publicPaths=new Set(['/login.html','/invite.html','/setup-admin.html','/favicon.ico']);
app.use(async(req,res,next)=>{if(req.path.startsWith('/api/auth/'))return next();if(req.path==='/api/health'||req.path.startsWith('/api/webhooks/meta'))return next();if(publicPaths.has(req.path))return next();const u=await requireUser(req,res);if(!u)return;if(req.path==='/onboarding.html'&&u.role!=='admin')return res.status(403).send('Admin access required');(req as any).relayosUser=u;next()});
app.use(async(req,res)=>{if(!checkOrigin(req,res))return;const u=(req as any).relayosUser||await currentUser(req),url=`http://127.0.0.1:${internalPort}${req.originalUrl}`,headers:any={};for(const[k,v]of Object.entries(req.headers)){if(v!==undefined&&!['host','content-length','cookie','x-relayos-operator-id'].includes(k))headers[k]=Array.isArray(v)?v.join(','):v}if(u)headers['x-relayos-operator-id']=u.id;let body:any;if(!['GET','HEAD'].includes(req.method)&&req.body!==undefined){headers['content-type']='application/json';body=JSON.stringify(req.body)}try{const r=await fetch(url,{method:req.method,headers,body,redirect:'manual'});r.headers.forEach((v,k)=>{if(!['transfer-encoding','content-length','set-cookie'].includes(k.toLowerCase()))res.setHeader(k,v)});res.status(r.status).send(Buffer.from(await r.arrayBuffer()))}catch(e:any){res.status(502).json({error:'backend_unavailable',detail:e.message})}});

await migrateAuth();await migrateKnowledge();await migrateHybridRag();await migrateCrm();await migrateCrmIntelligence();await migrateContinuity();
const child=spawn(process.execPath,['--import','tsx','server/index.ts'],{stdio:'inherit',env:{...process.env,PORT:String(internalPort)}});child.on('exit',code=>{console.error('RelayOS internal backend exited',code);process.exit(code??1)});
setTimeout(()=>app.listen(publicPort,()=>console.log(`RelayOS AI CRM intelligence + continuity gateway listening on ${publicPort}`)),500);
